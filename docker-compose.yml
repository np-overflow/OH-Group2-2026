services:
  backend:
    build: 
      context: ./backend
    restart: always

  frontend:
    build: 
      context: ./frontend
    network_mode: service:backend
    restart: always
    ports:
      - "3000:3000"
    depends_on:
      - backend

volumes:
